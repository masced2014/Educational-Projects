name: Security Check

on:
  push:
    branches:
      - main

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run Rust Audit
        uses: actions-rust-lang/audit@v1
        with:
          workingDirectory: rust_file_encrypt
